The two employees with the highest ever salaries are Tokuyasu Pesch with a salary of 158,220 and Honesty Mukaidono with a salary of 156,286.

=========SQL QUERY=========
SELECT e.first_name, e.last_name, MAX(s.salary) AS highest_salary FROM employees e JOIN salaries s ON e.emp_no = s.emp_no GROUP BY e.emp_no ORDER BY highest_salary DESC LIMIT 2
===========================

real	0m14.844s
user	0m0.221s
sys	0m0.072s

The two employees with the highest ever salaries are:

1. Tokuyasu Pesch with a salary of 158,220
2. Honesty Mukaidono with a salary of 156,286

=========SQL QUERY=========
SELECT e.first_name, e.last_name, MAX(s.salary) AS highest_salary FROM employees e JOIN salaries s ON e.emp_no = s.emp_no GROUP BY e.emp_no ORDER BY highest_salary DESC LIMIT 2
===========================

real	0m15.970s
user	0m0.222s
sys	0m0.069s

The two employees with the highest ever salaries are Tokuyasu Pesch with a salary of 158,220 and Honesty Mukaidono with a salary of 156,286.

=========SQL QUERY=========
SELECT e.first_name, e.last_name, MAX(s.salary) AS highest_salary FROM employees e JOIN salaries s ON e.emp_no = s.emp_no GROUP BY e.emp_no ORDER BY highest_salary DESC LIMIT 2
===========================

real	0m12.676s
user	0m0.214s
sys	0m0.072s

The two employees with the highest ever salaries are Tokuyasu Pesch with a salary of 158,220 and Honesty Mukaidono with a salary of 156,286.

=========SQL QUERY=========
SELECT e.first_name, e.last_name, MAX(s.salary) AS highest_salary FROM employees e JOIN salaries s ON e.emp_no = s.emp_no GROUP BY e.emp_no ORDER BY highest_salary DESC LIMIT 2
===========================

real	0m14.503s
user	0m0.222s
sys	0m0.070s

The two employees with the highest ever salaries are:

1. Tokuyasu Pesch with a salary of 158,220.
2. Honesty Mukaidono with a salary of 156,286.

=========SQL QUERY=========
SELECT e.first_name, e.last_name, MAX(s.salary) AS highest_salary FROM employees e JOIN salaries s ON e.emp_no = s.emp_no GROUP BY e.emp_no ORDER BY highest_salary DESC LIMIT 2
===========================

real	0m14.126s
user	0m0.217s
sys	0m0.069s

The two employees with the highest ever salaries are:

1. Tokuyasu Pesch with a salary of 158,220.
2. Honesty Mukaidono with a salary of 156,286.

=========SQL QUERY=========
SELECT e.first_name, e.last_name, MAX(s.salary) AS highest_salary FROM employees e JOIN salaries s ON e.emp_no = s.emp_no GROUP BY e.emp_no ORDER BY highest_salary DESC LIMIT 2
===========================

real	0m14.337s
user	0m0.219s
sys	0m0.068s

The two employees with the highest ever salaries are:

1. Tokuyasu Pesch with a salary of 158,220.
2. Honesty Mukaidono with a salary of 156,286.

=========SQL QUERY=========
SELECT e.first_name, e.last_name, MAX(s.salary) AS highest_salary FROM employees e JOIN salaries s ON e.emp_no = s.emp_no GROUP BY e.emp_no ORDER BY highest_salary DESC LIMIT 2
===========================

real	0m12.999s
user	0m0.217s
sys	0m0.070s

The two employees with the highest ever salaries are:

1. Tokuyasu Pesch with a salary of $158,220
2. Tokuyasu Pesch with a salary of $157,821

=========SQL QUERY=========
SELECT e.first_name, e.last_name, s.salary FROM employees e JOIN salaries s ON e.emp_no = s.emp_no ORDER BY s.salary DESC LIMIT 2
===========================

real	0m14.970s
user	0m0.223s
sys	0m0.075s

The two employees with the highest ever salaries are:

1. Tokuyasu Pesch with a salary of 158,220.
2. Honesty Mukaidono with a salary of 156,286.

=========SQL QUERY=========
SELECT e.first_name, e.last_name, MAX(s.salary) AS highest_salary FROM employees e JOIN salaries s ON e.emp_no = s.emp_no GROUP BY e.emp_no ORDER BY highest_salary DESC LIMIT 2
===========================

real	0m12.905s
user	0m0.222s
sys	0m0.072s

The two employees with the highest ever salaries are:

1. Tokuyasu Pesch with a salary of 158,220.
2. Honesty Mukaidono with a salary of 156,286.

=========SQL QUERY=========
SELECT e.first_name, e.last_name, MAX(s.salary) AS highest_salary FROM employees e JOIN salaries s ON e.emp_no = s.emp_no GROUP BY e.emp_no ORDER BY highest_salary DESC LIMIT 2
===========================

real	0m13.099s
user	0m0.220s
sys	0m0.071s

